<div class="container">
  <% @results.each do |result| %>
  <h1>Good Morning</h1>
  <p style="color: gray"><%= Date.today %></p>
    <div class="container">
      <div class="row">
        <div class="col-lg-6">
          <div class="card">
            <%= link_to result_path(result), class: "no-underline" do %></p>
            <div class="card-header">
            <h1><%= Date.today.day %></h1>
            </div>
            <p><b>Result ID </b>
            <p><b>Patient Name</b> <%= result.user.first_name %></p>
            <% result.infections.each do |infection| %>
            <p><li><%= infection.id %> | <%= infection.name %></li></p>
            <% end %>
            <br>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
