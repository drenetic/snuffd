<h2>Result from <%= @result.test_date.strftime("%d %b %y") %></h2>

<div>

 <% @result.results_infections.each do |infection|  %>
  <div>
    <h3><%= link_to Infection.find(infection.infection_id).name, result_results_infection_path(infection) %> <%= infection.is_treated ? "👍" : "💊"%></h3>
  </div>
 <% end %>

</div>
<% if user_signed_in? %>
  <%= link_to "Generate Link", result_link_path(@result) %>
<% end %>
